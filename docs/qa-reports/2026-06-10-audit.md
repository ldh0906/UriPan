# UriPan QA 감사 — 2026-06-10

기준: `afe9fb6` (v1.2.0, origin/main). Baseline: `flutter analyze` 무이슈, 테스트 162개 전부 통과.
방식: Codex 읽기전용 감사 3개 병렬 (A=인증/초대, B=UI/날짜, C=상태/실시간/리마인더). 06-08 감사에서 수정된 항목은 제외.

## P1 (1건)

| ID | 제목 | 위치 | 근본 원인 |
|----|------|------|-----------|
| QA-C1 | 로그아웃 시 로컬 알림 미취소 | `board_home_screen.dart:189,608,618` | 로그아웃 3개 경로가 `auth.signOut()`만 호출. 알림 취소는 토글 OFF나 activeBoard null sync에서만 발생 → 활성 보드 상태에서 로그아웃하면 이전 보드 예약 알림 잔존 |

## P2 — 수정 대상 (11건)

| ID | 제목 | 위치 | 근본 원인 |
|----|------|------|-----------|
| QA-A1 | 가입 직후 자동로그인인데 "로그인해주세요" 배너 + keepSignedIn 미저장 | `login_screen.dart:72,93`, `auth_gate.dart:33` | `_signUp()`이 성공 메시지를 띄우지만 confirm-off라 즉시 세션 생성 → AuthGate가 홈으로 전환. 가입 경로는 `_setKeepSignedIn` 미호출 |
| QA-A3 | 프로필 수정 후 화면의 멤버명/작성자명 미갱신 | `board_session_controller.dart:191`, `board_home_screen.dart:353` | `updateMyProfile()`이 `_myProfile`만 갱신, `_members`/`_items` 캐시는 다음 전체 로드까지 옛 이름 |
| QA-A4 | 초대코드 패널이 다른 관리자의 취소/재생성 미반영 | `board_realtime_subscription.dart:29`, `board_header.dart:217` | `board_invites` 미구독, `_activeInvite`는 초기 로드나 본인 조작 때만 갱신 → stale 코드 복사 가능. (클라 측 완화: 패널 진입/복사 전 재조회) |
| QA-A5 | 일부 DB 오류 한국어 매핑 누락 | `board_home_screen.dart:524` | `invite_not_found`, `task_not_found_or_no_access`, `notice_confirmation_required` 미매핑 → 원문 노출 |
| QA-B1 | 다일 일정 종료일이 카드/상세에 미표시 | `board_item_card.dart:126`, `item_detail_sheet.dart:201` | 일정 라벨이 `startsAt`만 사용, `dueAt` 종료일은 캘린더에만 반영 |
| QA-B2 | 편집 시트 담당자 드롭다운 "없음" 표시인데 저장은 기존 담당자 유지 | `board_action_sheets.dart:173,278,446` | 탈퇴한 담당자 ID가 members에 없으면 표시값 `''`인데 submit은 원본 `_assignedToId` 전송 → UI/저장 불일치 |
| QA-B3 | 범위 밖 날짜 항목 편집 시 date picker initialDate가 first/lastDate 제약 위반 | `board_action_sheets.dart:153,464,496` | picker 범위가 now-365d~+3y 고정인데 편집 초기일은 기존 항목 날짜 그대로 |
| QA-B4 | `startOfCalendarWeek`만 UTC 입력 toLocal 미정규화 | `today_board_screen.dart:180` | `_calendarDateOnly` 등과 달리 toLocal 없이 잘라 UTC 경계에서 주 시작 어긋남 가능 |
| QA-C2m | 댓글/확인 realtime 이벤트가 타 보드 변경에도 전체 새로고침 유발 | `board_realtime_subscription.dart:40,46` | `item_confirmations`/`item_comments` 구독에 board_id 필터 없음 → 무조건 `onItemsChanged()`. (클라 측 완화: payload item_id가 현재 보드 항목일 때만 refresh) |
| QA-C4 | 컨트롤러가 dispose 후 notifyListeners 호출 가능 | `board_session_controller.dart:63,394` | 비동기 load/refresh 완료 시 dispose 상태 미확인, `_stateRequestId`는 stale만 차단 |
| QA-C5 | MemoryBoardRepository 멤버가 보드별 미분리 | `board_repository.dart:109,152,272` | `_members` 전역 단일 리스트, `loadMembers(boardId)`가 boardId 무시 → Supabase 동작과 불일치 |

## 연기 (스키마/마이그레이션 필요, 3건)

| ID | 제목 | 사유 |
|----|------|------|
| QA-A2 | 탈퇴 멤버 이름 UUID/'???' 표시 | profiles RLS상 구조적. 표시명 스냅샷 컬럼 or soft-delete(`left_at`) or security-definer view 설계 결정 필요 |
| QA-C2 (완전판) | 댓글/확인 테이블 board_id denormalize + realtime 필터 | 마이그레이션 + 트리거 필요. 이번엔 클라 측 완화(QA-C2m)만 적용 |
| QA-C3 | `boards` 테이블 realtime 미구독 (보드 이름/정원 변경 비전파) | publication에 boards 추가 마이그레이션 필요. admin joined_at 변조 방지 트리거와 같은 배치 권장 |

## 깨끗하게 확인된 영역
- RPC 파라미터 ↔ 마이그레이션 시그니처 전부 일치 (leave_board, set_board_nickname, invite 3종)
- `filterTaskBoardItems`, `moveCalendarMonth` 클램프, 일요일 시작 주, `calendarWeekBars` 클리핑, `isForDate` 로컬 정규화, `friendlyRelativeTime` 미래시각
- `buildReminderPlan` D1/D2 정책·30일 캡, 상세시트 per-item 댓글 구독 dispose/mounted 가드
- main.dart cold-start signOut try/catch (06-08 픽스 유지 확인)
